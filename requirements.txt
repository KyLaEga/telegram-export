# Рантайм-зависимости Telegram Export (GUI + движок).
# Версии зафиксированы под текущее проверенное окружение venv/ (Python 3.12).
Pyrogram==2.0.106
TgCrypto==1.2.5
PySide6==6.11.1
# QR-вход (нативное «Подключить устройство»): чистый Python, матрицу QR рисует
# QPainter в GUI, поэтому Pillow НЕ нужен (qrcode работает и без него).
qrcode==8.2
# Скачивание комиксов по ССЫЛКАМ: сборка PDF из картинок любых форматов (JPEG/PNG/
# WebP/GIF). Импортируется ЛЕНИВО — без Pillow CBZ работает всегда, а PDF умеет JPEG
# через встроенный безбиблиотечный сборщик (link_export._save_pdf_jpeg_only).
Pillow==12.2.0
# Транзитивные зависимости Pyrogram (фиксируем явно для воспроизводимой сборки):
pyaes==1.6.1
PySocks==1.7.1
