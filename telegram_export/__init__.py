"""Packaging entry-point package for the Briefcase macOS app.

The real application lives in the `gui` package; this thin package exists so the
Briefcase app module name (`telegram_export`) matches a bundled source package.
"""

__version__ = "0.3.1"
